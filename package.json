{
  "name": "local-agent-orchestrator",
  "version": "0.1.0",
  "description": "Nucleo do AI Orchestrator: runtimes gerenciados, contas isoladas, processos, git e banco. Consumido pelo aplicativo desktop em apps/desktop.",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:tests": "tsc -p tsconfig.test.json",
    "clean": "node -e \"const f=require('fs');for(const d of ['dist','dist-tests'])f.rmSync(d,{recursive:true,force:true})\"",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "pretest": "npm run build && npm run build:tests",
    "test": "node --test \"dist-tests/tests/**/*.test.js\""
  },
  "keywords": [
    "orchestrator",
    "codex",
    "claude-code",
    "electron"
  ],
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
