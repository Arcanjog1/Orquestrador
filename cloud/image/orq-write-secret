#!/bin/sh
# Writes stdin to a file only this user can read.
#
# The token arrives over the exec's stdin rather than as an argument, so it is
# never visible in the container's process list.
set -eu
umask 077
target="$1"
mkdir -p "$(dirname "$target")"
cat > "$target"
chmod 0600 "$target"
