node_modules/
dist/
dist-tests/
# The coordinator's build output, from tsconfig.coordinator.json.
dist-coordinator/
*.tsbuildinfo

# Runtime data produced by the orchestrator
.sessions/
logs/
profiles/

# Local configuration may contain machine-specific paths
config.json

# OS noise
Thumbs.db
.DS_Store

# Spike report is generated per-machine
spike/spike-report.txt

# Local agent/CLI state must never be committed: these files carry account
# identifiers, machine fingerprints and, in other layouts, credentials.
.claude.json
.claude.json.backup*
.claude/
.credentials.json
.codex/
relative/
profiles/**/.claude.json

# Electron / desktop build output
apps/desktop/dist/
apps/desktop/dist-renderer/
apps/desktop/release/
apps/desktop/overflow-results/
# electron-builder writes here when it is run from the repository root
release/
out/
